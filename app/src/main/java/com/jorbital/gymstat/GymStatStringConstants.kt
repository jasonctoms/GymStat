package com.jorbital.gymstat

class GymStatStringConstants {
    companion object  {
        //Shared Preference keys (important to note that these strings are defined in two places,
        //also in res/xml/pref files)
        const val WEIGHT_UNIT = "weight_unit"

        //Shared Preference values (important to note that these strings are defined in two places,
        //also in the strings resource file)
        const val POUNDS = "Pounds"
        const val KILOGRAMS = "Kilograms"


        //Bundle keys
        const val SELECTED_ROUTINE = "SELECTED_ROUTINE"
    }
}