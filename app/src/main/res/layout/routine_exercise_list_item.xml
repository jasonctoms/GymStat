<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools">

    <data>
        <variable
            name="vh"
            type="com.jorbital.gymstat.views.RoutineExerciseListAdapter.RoutineExerciseViewHolder"/>
    </data>

    <android.support.constraint.ConstraintLayout
        android:id="@+id/routineExerciseItem"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <TextView
            android:id="@+id/routineExerciseName"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@color/colorPrimary"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            tools:text="Exercise Name"/>

        <TextView
            android:id="@+id/numberOfSets"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@color/colorPrimary"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintLeft_toRightOf="@+id/routineExerciseName"
            app:layout_constraintTop_toTopOf="parent"
            tools:text="x XX sets"/>
    </android.support.constraint.ConstraintLayout>
</layout>